Drop background films here.

  hero.mp4     -> hero background
  finale.mp4   -> closing CTA background

Each one is picked up automatically. If the file is absent the still
image behind it is used instead and nothing breaks.

Suggested: 8-15s silent loop, 1080p, under ~4 MB, dark and slow.
